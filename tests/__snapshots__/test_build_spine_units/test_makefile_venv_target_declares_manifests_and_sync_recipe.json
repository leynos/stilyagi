{
  "header": ".venv: pyproject.toml uv.lock $(WORKSPACE_MANIFEST) Cargo.lock",
  "recipe": [
    "UV_VENV_CLEAR=1 $(UV_ENV) $(UV) venv",
    "$(CARGO_BUILD_ENV) $(UV_ENV) $(UV) sync --group dev"
  ]
}
